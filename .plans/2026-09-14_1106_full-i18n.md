# Full i18n: CLDR-grade formatting, messages, RTL, per-locale live board

**Status:** Phases 0, 1, 2, 5 and 6 done, 3 done except the `/formats` live ticker. Locale URLs; numbers, plurals, units, lists, relative time, durations, dates, zones and ranges are exact to Intl (33,301 cases). Every UI string is in 14 catalogs, dates follow the viewer (cookie > cf > UTC, browser-corrected). Next: deploy, then Phase 4 (collation, display names, text, input). Open: decision 13 (UI text via gsx renderers, Phase 5b) ·
**Created:** 2026-09-14 11:06 · Phase 0 spike code: ported into the repo, then deleted (2026-09-14; see "Where the Phase 0 code went")

## Decisions made (2026-09-14, your answers)

- **Phase 0: go now**, scratchpad only.
- **v1 locales: the bigger set.** `en` (default), `en-IN`, `de`, `fr`, `es`, `pt` (`pt-BR`, `pt-PT`), `ar`, `he`, `ja`,
  `zh` (`zh-Hans`, `zh-Hant`), `hi` and `ru`. All are in Chrome/Workers' full-data 57, so the workerd oracle covers
  them. Phase 0 still probes `is` as the known gap. More locales are catalog + generator-list work.
- **Browser JS: yes, guarded** (decision 6). This is an AGENTS.md exception for one viewer-local `<time>` behaviour.
- **Sequencing:** Phases 1+ after one-app Phase 2 (decision 12).
- **Engine: C (own CLDR generator + stdlib runtime), confirmed by Phase 0.** The prototype matched 392/392 oracle
  cases for all 14 locales, gave byte-identical output under TinyGo on workerd, and costs ~3.2 KB gzip per locale.
- **Spike code:** kept in `.spikes/i18n-phase0/` (your OK, 2026-09-14), then ported and deleted once every useful part
  was in the repo (your OK, 2026-09-14 15:55). Other developers never saw it: `.git/info/exclude` only covers one
  clone.

## Goal

The app gets **real** i18n, the feature set of ICU / JavaScript `Intl`, done on the server in Go:

- locale negotiation with fallback chains
- ICU-style messages with plurals, ordinals and select
- numbers: decimal, percent, compact, numbering systems
- **currency**: money as integer minor units, CLDR patterns, currency digits
- units and measurement systems
- **dates and times in the viewer's time zone**: styles, skeletons, intervals, 12/24 h
- **relative time that stays live** ("in 3 days", "2 min ago")
- durations and lists ("A, B und C")
- display names (languages, regions, currencies)
- collation (locale-aware sorting)
- text handling: case mapping, NFC, grapheme-safe truncation, bidi
- localized input parsing
- RTL
- SEO alternates

All of it has to work in every place the app renders:

- server-rendered pages
- htmx fragments
- hx-ws pushes
- error responses

It must keep running under TinyGo on Workers and under `go run .`, with identical output. Adding a locale should
mean adding one catalog and listing the locale for CLDR generation, with no view or handler changes. Tests must fail on
a hard-coded string, a missing translation, an unprefixed URL, or output that disagrees with ICU.

## Decisions needed (recommendations first)

1. **Locales for v1.** Recommend:
   - `en` (default, with `en-IN` for Indian digit grouping)
   - `de` (text expansion, `1.234,56 €`)
   - `ar` (RTL, six plural forms, Arabic-Indic digits)
   - `ja` (CJK, no plurals, era calendar available)

   Pseudo-locales `en-XA` (accented, ~40% longer) and `ar-XB` (RTL) are test-only.
2. **URL scheme.** Recommend a path prefix per locale (`/de/board`). The default locale stays unprefixed and is
   `x-default`. No redirects based on Accept-Language (Google's guidance; Googlebot sends none). An explicit choice
   sets a cookie, and only a bare `/` honours it. Unknown prefixes give 404.
3. **Server formatting engine.** Recommend **C + oracle**. Engine D (Cloudflare's own `Intl`) was the obvious
   shortcut, but Cloudflare's `Intl` isn't full:
   - **What Cloudflare has.** Workers support "all of the standard built-in objects supported by the current Google
     Chrome stable release" ([web standards](https://developers.cloudflare.com/workers/runtime-apis/web-standards/)),
     so `Intl` is there.
   - **What's missing** (reported by [cldr-is](https://github.com/gudrodur/cldr-is), confirm in Phase 0):
     - Workers embed **Chromium's filtered ICU data**, the same as desktop Chrome, measured identical on local workerd
       and the live edge.
     - Chromium keeps full data only for languages its own UI is translated into.
     - For other locales (Icelandic, for example), `DateTimeFormat`, `NumberFormat`, `RelativeTimeFormat`,
       `ListFormat` and `Collator` silently fall back to `en-US`; only `PluralRules` is right.
   - **Time zones** ([workerd#7256](https://github.com/cloudflare/workerd/issues/7256), open): workerd's embedded
     tzdata predates IANA 2026c, so Casablanca resolves to the wrong offset from 2026-09-20.
   - **So D can't be "full".** It covers our v1 locales (de, ar, ja are Chrome UI languages) but not every locale we
     might add. A D+C hybrid would be two engines rendering the same pages.
   - **D. Runtime `Intl` via `syscall/js`** is therefore rejected as the engine. It stays useful as the **oracle** for
     Chrome-covered locales, and CLDR test data covers the rest.
   - The **C + oracle** option, in detail:
   - **C. Our own CLDR generator.** `cmd/cldrgen` reads [cldr-json](https://github.com/unicode-org/cldr-json)
     48.2.1 at generate time. It writes per-locale Go tables for the supported locales only, plus a pure-Go runtime
     (future `kit/i18n`). This gives TinyGo-safe code, native = Workers output, and a controlled size.
   - **Oracle: workerd's V8 `Intl`** (full ICU, assuming Phase 0 confirms it). A local-only shim in `workerd/`
     generates golden outputs, and `go test` compares the Go engine against them. It's the same engine family as
     production, with no Chrome and no Node.
   - **Rejected as the engine:**
     - `golang.org/x/text` v0.42.0: no dates, relative time, lists, units or intervals. Its numbers and currency are
       partial, though `collate` and `display` stay candidates for those two features.
     - `go-playground/locales` v0.14.1: CLDR 36.0.1 (2019), `float64` currency, no relative time, lists, units or
       display names.
     - `go-i18n` v2.6.1: messages only, and it links `text/template`.
   - **Alternative D: call workerd's `Intl` at runtime via `syscall/js`.** It adds zero wasm size and gives full ICU,
     but `go run .` and `go test` have no `Intl`, so native and Workers output would differ. Rejected unless C blows
     the size budget.
4. **Message syntax.** Recommend **ICU MessageFormat 1** in `locales/{locale}.toml`: `plural`, `selectordinal`,
   `select`, and `number`/`date`/`time` arguments with skeletons. Translators and tools know it. It's parsed at
   generate time into typed Go methods, so there's no runtime parser and a missing key is a compile error. MF2 is the
   newer Unicode standard but has little tool support yet.
5. **Viewer preferences.**
   - Precedence: explicit setting (cookie) > Cloudflare's `request.cf` (`Timezone`, `Country` via workers-go
     `fetch.NewIncomingProperties`) > locale defaults > UTC.
   - Preferences: locale, time zone, display currency, hour cycle, first day of week, measurement system.
   - A settings dialog built from gsxui components.
   - `go run .` has no `cf`, so it falls back to locale defaults + UTC.
6. **Viewer-local enhancement in the browser.** Fragments are cached in the Room and pushed later, so server-rendered
   "2 min ago" goes stale, and the server only guesses the viewer's time zone (from `cf`). Recommend:
   - Go renders the finished text inside `<time datetime>` (crawlable).
   - A small behaviour re-formats every `<time>` with `Intl.RelativeTimeFormat` / `Intl.DateTimeFormat`, using the
     page's `lang` and the browser's real time zone. It runs on load, after each htmx swap and hx-ws push, and on a
     timer. This is the same pattern as GitHub's `relative-time` element.
   - **This needs a JS exception to AGENTS.md** (JS today is only the Worker entry, the DO and local shims).
   - The alternatives:
     - A server-authored schedule (`[instant, text]` pairs, JS only swaps text): byte-identical with engine C, but no
       real viewer time zone.
     - htmx `hx-trigger="every 60s"` re-rendering: no JS, but ~17 Worker requests/s per 1,000 tabs.
7. **Calendars.** Recommend Gregorian in v1, with the `-u-ca-` extension (Japanese era, Islamic, Buddhist) in a later
   phase once the generator exists. Numbering systems (`-u-nu-`) are in v1.
8. **Currency and unit conversion.** Recommend: formatting only.
   - Amounts carry their own ISO 4217 code, and we never convert money: exchange rates are data, not i18n.
   - Units convert only where a preference asks (km ↔ mi, °C ↔ °F) and for display only.
9. **Translations source.** Recommend hand-maintained catalogs with `en` as the source. Machine drafts (Gemini or
   Workers AI via fnox) would be ⚠ outward-facing on every run.
10. **gsxui hard-coded English.** Recommend passing labels as attributes where components forward them, and ⚠ upstream
    gsxui issues for label props elsewhere. Aria-labels such as "Close", "Loading" and "Go to next page" are affected,
    and gsxui's `docs/jsx-parity.md` lists calendar i18n as a gap. Don't edit the vendored `ui/`. Calendar waits for
    an upstream locale parameter, or we contribute one using our engine.
    gsx renderers (decision 13) don't help here: they never reach literals inside vendored components.
    **Filed upstream (2026-09-14, your OK):** [gsxui#31](https://github.com/gsxhq/gsxui/issues/31) labels,
    [gsxui#30](https://github.com/gsxhq/gsxui/issues/30) calendar locale,
    [gsxui#32](https://github.com/gsxhq/gsxui/issues/32) RTL physical classes.
11. **Size budget.** Workers limits compressed script size by plan (verify our plan on the limits page). Today
    `app.wasm` is 2.05 MB raw / 660 KB gzip. Recommend a budget of +800 KB gzip for 4 locales, including tzdata,
    enforced by a `mise run check` assertion.
12. **Sequencing.** Recommend Phase 0 now (scratchpad only). The rest follows
    [one-app](done/2026-09-14_1037_one-app-template-and-kit.md) Phase 2, so we translate the merged pages once. The engine
    lands as `kit/i18n` with one-app Phase 3, and one-app Phase 7's ⚠ deploy ships it.
13. **UI text through gsx `[renderers]`** (open; suggested by gsx's author Jackie Li, 2026-09-14). Recommend: adopt,
    generated by msggen, **keeping typed arguments**. Work is Phase 5b.
    - **Today:** `{ M(ctx).BoardTitle() }`, 103 call sites in `views/*.gsx`.
    - **Proposed markup:** `{ msg.BoardTitle }`, `aria-label={msg.BoardDecrement}`,
      `{ msg.BoardVersion(float64(b.Version)) }`. No `M(ctx)` in components; the renderer reads the locale from the
      render `ctx`.
    - **How** ([renderers](https://github.com/gsxhq/gsx/blob/v0.1.0/docs/guide/config.md#renderers-type-directed-value-rendering),
      [translation keys pattern](https://gsxhq.github.io/guide/patterns/i18n-keys.html)): msggen also generates
      - `locales.Key`: a struct over the catalog index, with no `String` method. If the `gsx.toml` registration goes
        missing, `gsx generate` fails instead of printing a raw key. It's a `var` per key in a generated `locales/msg`
        package (static data, no init func).
      - `locales.Message` for keys with arguments: constructors with today's typed parameters
        (`func BoardVersion(v float64) Message`), so MF1 argument kinds stay compile-time checked. The pattern's
        `struct{Key; Args []any}` would lose that.
      - Renderers `locales.RenderKey(ctx, Key) string` and `locales.RenderMessage(ctx, Message) string`, registered
        in `gsx.toml`, covering en-XA pseudo mode (`PseudoMessages` moves from `views` to `locales`).
      - A missing key stays a compile error, and a missing translation still comes from the CLDR parent chain. We don't
        use the pattern's "render the key itself" fallback.
    - **Where renderers don't apply:** gsx never applies them to a plain component argument. That covers string props
      (`<Layout title>`, the gsxui toast's `description=`), handlers, and message arguments.
      - Components we own take `locales.Message` props instead of strings (Layout title and description).
      - Everything else calls `msg.X.In(ctx)` / `locales.For(ctx)`.
    - **Same mechanism for values (Phase 4):** `i18n.Money` → formatted string, and possibly a `UserText` type that
      applies `Isolate`. Builtins (`int64`) can't be registered, so plain numbers keep `Num(ctx, v)`. A registration
      has one result type used in both text and attributes, so text types return `string`, and markup such as
      `<time datetime>` stays a component (`LocalTime`).
    - **Checked in pinned gsx v0.1.0 source:**
      - `[renderers]` shipped (ROADMAP "renderers registry - SHIPPED"), including the `context.Context` form.
      - Codegen emits a direct call, `RenderKey(ctx, (expr))` (`internal/codegen/renderers.go` `applyRenderer`), so
        there's no reflection and it's TinyGo-safe.
      - Applied in text, attribute, URL-attribute, conditional-attribute and fallthrough-attrs positions.
      - Renderers don't chain. Type-parameter holes and runtime `any` values (attr maps, spreads) don't see them.
      - LSP hover doesn't show the applied renderer yet (gsx ROADMAP follow-up).
    - **Tests stay:** `TestNoHardcodedText` (pseudo), `TestGeneratedUpToDate`, `TestShippedLocalesComplete`.
      `mise run rename` must rewrite the module path in the new `gsx.toml` entries.

## Ground rules

- Same as the app: no Node, TinyGo for everything on Workers, plain-path routes + `allow()`, and gsx for all markup.
  Credentials come from fnox. GUI is gsxui only. Deploys, external APIs and upstream issues need your OK.
- Every tooling step cites its upstream guide. Demo-first: upstream examples run in the scratchpad first, then **our
  own demo**, a `/formats` showcase page that grows with each phase before the app pages use it.
- **The server HTML is always complete and localized.** Crawlers that don't run JS see finished text, and so do AI
  fetchers, link previews and no-JS visitors. The browser's `Intl` only *enhances* what depends on the viewer: their
  time zone, live relative time, client-side sort and filter. It always gets the page's locale
  (`document.documentElement.lang`), never the browser default. See "Browser `Intl` and Google" below.
- CLDR data is read at generate time only. The generated tables are committed, like vendored data, and never
  hand-edited.

## Upstream guidance

- **CLDR**
  - [cldr-json](https://github.com/unicode-org/cldr-json) 48.2.1 (latest tag, checked 2026-09-14).
  - [LDML spec](https://unicode.org/reports/tr35/) parts: Numbers (patterns, currencies, compact), Dates (patterns,
    skeletons, intervals, time zone names, relative fields), General (units, lists, display names), Plural rules,
    Collation.
- **ICU MessageFormat:** [ICU user guide: formatting messages](https://unicode-org.github.io/icu/userguide/format_parse/messages/).
- **IANA time zones in Go:** [`time/tzdata`](https://pkg.go.dev/time/tzdata) embeds the database. Check TinyGo's
  support and size in Phase 0.
- **gsx**
  - [Context](https://github.com/gsxhq/gsx/blob/v0.1.0/docs/guide/syntax/context.md): ambient `ctx` in every
    component, with locale as a listed use.
  - [Pipeline filters](https://github.com/gsxhq/gsx/blob/v0.1.0/docs/guide/config.md): `func(ctx, value, args…)`
    registered in `gsx.toml`, e.g. `{ n.CreatedAt |> relTime }`.
  - [Type renderers](https://github.com/gsxhq/gsx/blob/v0.1.0/docs/guide/config.md#renderers-type-directed-value-rendering):
    `[renderers]` maps a named type to `func(ctx, T) R`, applied wherever a value of that type renders.
    [Translation keys pattern](https://gsxhq.github.io/guide/patterns/i18n-keys.html) (newer docs than v0.1.0; it
    uses only the v0.1.0 feature). Decision 13.
- **gsxui RTL** (`.upstream/gsxui/site/pages/rtl.gsx`, pin c7fd6a8)
  - `dir="rtl"` on `<html>` is enough: logical classes, `rtl:rotate-180`, direction-aware floating UI, mirrored keys.
  - Sheet, Drawer and Sidebar `side` stays physical.
  - Fonts: Noto Sans Arabic as a fallback after Geist.
- **workers-go** `cloudflare/fetch/property.go`: `IncomingProperties.Timezone`, `.Country`
  (`_examples/incoming`).
- **Google**
  - [Multi-regional and multilingual sites](https://developers.google.com/search/docs/specialty/international/managing-multi-regional-sites):
    separate URLs per language, no redirects based on perceived language, content language read from visible text.
  - [hreflang](https://developers.google.com/search/docs/specialty/international/localized-versions): reciprocal
    alternates plus `x-default`.
- **Cloudflare**
  - [Workers limits](https://developers.cloudflare.com/workers/platform/limits/): script size, CPU time.
  - [DO WebSocket Hibernation](https://developers.cloudflare.com/durable-objects/best-practices/websockets/):
    `acceptWebSocket(ws, tags)`, `getWebSockets(tag)`.

## Browser `Intl` and Google

The browser's `Intl` works **with** Google, as long as it only enhances server HTML and never replaces it:

- **Google renders JS** with evergreen Chromium ([JavaScript SEO basics](https://developers.google.com/search/docs/crawling-indexing/javascript/javascript-seo-basics)).
  Client-formatted text can be indexed, but rendering can lag the crawl. Google still recommends server-side rendering
  because "not all bots can run JavaScript".
- **Not everything we want to reach runs JS.**
  - The search plan wants Gemini grounding and the user-triggered `Google-Agent` / `Google-GeminiNotebook` fetchers,
    which need pages that "work as plain HTML".
  - Other search engines, AI crawlers and link previews mostly don't render JS either.
  - So finished text has to be in the server HTML, and a client-only formatting design fails them.
- **The renderer's locale and time zone are Google's, not the visitor's.** Googlebot crawls from US IPs with no
  Accept-Language, and the renderer's `navigator.language` and time zone aren't documented. If client code used the
  browser default, a `/de/` page could be indexed with US-English dates: mixed-language content on a German URL. So
  client `Intl` always takes the page's `lang`. The time zone difference is harmless for indexing (a time shifts, the
  language doesn't).
- **Browsers have the same locale gaps as Workers.** Desktop Chrome/Edge drop full data for languages their UI isn't
  translated into, and Google's renderer is Chromium. For such a locale, client `Intl` would replace our correct server
  text with `en-US` text, both for visitors and in Google's rendered page. So the behaviour **feature-detects** before
  re-formatting (`Intl.DateTimeFormat.supportedLocalesOf([lang])` plus a probe that the resolved locale isn't `en`) and
  otherwise leaves the server text alone.
- **Avoid visible reflow.** If the client reformats server text into something different, the page flickers and shifts
  layout. The re-format targets only `<time>` and explicitly marked values, and only changes what depends on the
  viewer (time zone, elapsed time).
- **Verify, don't assume.** Phase 8 checks `/de/` and `/ar/` in Search Console's URL Inspection → "View crawled page"
  (rendered HTML + screenshot): language, digits and dates as expected.

## Feature matrix (what "full" means; each row gets goldens and a `/formats` section)

| Area | v1 scope | Engine data (CLDR) | Oracle |
|---|---|---|---|
| Locale | BCP 47 parse, likely subtags, parent chains (`de-AT→de→root`), Accept-Language matching, `-u-nu/-hc/-fw/-ca` | supplemental likelySubtags, parentLocales | `Intl.getCanonicalLocales`, `Intl.Locale.maximize` |
| Plurals | cardinal, ordinal, range | plurals, ordinals, pluralRanges | `Intl.PluralRules` (incl. `selectRange`) |
| Messages | MF1 plural / selectordinal / select + formatted args, named placeholders | (catalogs) | ICU examples |
| Numbers | decimal, percent, compact short/long, signs, min/max digits, rounding, Indian grouping, numbering systems | numbers, numberingSystems | `Intl.NumberFormat` |
| Currency | standard, accounting, symbol / narrowSymbol / code / name (plural), ISO digits, cash rounding, currency spacing, region default | currencies, currencyData | `Intl.NumberFormat` style currency |
| Units | length, mass, temperature, volume, speed, digital, duration units; long/short/narrow; metric/US/UK | units, unitPreferenceData, measurementData | `Intl.NumberFormat` style unit |
| Dates/times | date and time styles, skeletons → best pattern, hour cycle, day periods, eras, intervals ("3.–5. Sept. 2026") | ca-gregorian, dateFields, timeData | `Intl.DateTimeFormat` (+ `formatRange`) |
| Time zones | IANA via tzdata, localized names: GMT offset, generic/specific, exemplar city | timeZoneNames, metaZones | `timeZoneName` options |
| Relative time | numeric always/auto ("yesterday"), long/short/narrow, live schedule (decision 6) | dateFields relative | `Intl.RelativeTimeFormat` |
| Durations | "1 h, 5 min", digital "1:05:00" | units + durationUnits | `Intl.DurationFormat` |
| Lists | conjunction / disjunction / unit, long/short/narrow | listPatterns | `Intl.ListFormat` |
| Display names | languages (switcher shows "Deutsch", "العربية"), regions, scripts, currencies | localenames | `Intl.DisplayNames` |
| Week data | first day, weekend, min days | weekData | `Intl.Locale.getWeekInfo` |
| Collation | locale sort (de ä, sv å, ja kana), accent/case-insensitive compare | spike `x/text/collate` vs root collation + tailorings | `Intl.Collator` |
| Text | locale case mapping (tr İ), NFC before length checks, grapheme-safe truncation, bidi (`dir="auto"`, `<bdi>`) | `x/text/unicode/norm`, `x/text/cases`? (size check) | `toLocaleUpperCase`, `Intl.Segmenter` |
| Input | parse localized decimal and currency amounts (`1.234,56`); native `type=date/time/number` inputs submit canonical values | numbers symbols | round-trip tests |

## Inventory today

- **Views:** `layout.gsx`, `home.gsx`, `board.gsx`, `fragments.gsx` and `theme.gsx` are all literal English.
- **Handlers:** `http.Error` strings in `board.go` and `main.go` are English.
- **Room DO:** `worker/room.mjs` renders `N online` in JS and broadcasts one HTML payload to every socket, whatever
  its locale.
- **Layout:** `<html lang="en">` is hard-coded, with no `dir`, and the nav uses the physical `mr-auto`.
- **Formatting:** the board value is an unformatted int. `Note.CreatedAt` is a pre-formatted string, and
  `NowFragment` shows raw RFC 3339.
- **Money and units:** the app has none today. `/formats` demonstrates them, and the kit supports them.
- **Fonts:** Geist is Latin only.
- **SEO:** no hreflang, canonical or translated head (see the
  [search plan](2026-09-14_0938_search-indexing-google-gemini.md) Phase 2).

## Design

- **Request context.**
  - A middleware strips `/{locale}` (plain-path ServeMux) and resolves preferences (decision 5).
  - It stores an `i18n.Context` (locale, time zone, currency, hour cycle, units, week) in `r.Context()`.
  - `writeNode` renders with it, so components call `i18n.From(ctx)` or pipeline filters. No locale parameters.
- **Engine layout.**
  - `kit/i18n`: runtime, stdlib only.
  - `kit/i18n/cldr`: generated per-locale tables.
  - `cmd/cldrgen`: fetches the pinned cldr-json tag, keeps only the listed locales and their parents, and writes Go.
  - `cmd/msggen`: `locales/*.toml` → typed Go.
  - `mise run i18n` runs both.
- **Money.** `i18n.Money{Minor int64, Currency string}`, never a float. Rounding and digits come from `currencyData`.
- **Time.**
  - Instants are stored as UTC RFC 3339.
  - Rendering uses the viewer's zone. That needs tzdata, so the zone database is embedded; Phase 0 measures the size
    under TinyGo.
  - Every rendered date is `<time datetime>`, and every rendered number that matters is `<data value>`.
- **URLs.** One helper builds every in-app URL: `href`, `hx-get`/`hx-post`, `hx-ws:connect`, form `action`.
  `/static/`, `/assets/` and `/gsxui/` stay unprefixed. Fragments inherit the locale through their prefixed URL.
- **Direction.**
  - `<html lang dir>` comes from the locale.
  - The locale switcher is links that opt out of boost (`hx-boost:inherited="false"`), because boosted navigation
    swaps only `<body>`.
  - Each switcher item is labelled in its own language with `lang` and `hreflang`.
- **Live board.** One Room per topic, so presence stays one count.
  - `/live/{topic}?locale=de` is validated in `index.mjs` against Go's list, with a shared-rule test.
  - The Room calls `acceptWebSocket(server, [locale])`.
  - Go publishes one fragment per locale in one request, and the Room keeps `last` per locale and sends each tag its
    own HTML.
  - Presence becomes count-only, with a translated label rendered by Go.
  - **Time zones are not per socket.** Pushed fragments show relative times (a schedule with absolute instants works in
    any zone). The absolute tooltip uses a zone-free format with a UTC label, or the schedule's final text.
- **Oracle and goldens.**
  - `workerd/intl-oracle.mjs` (local-only shim) plus `mise run i18n:golden` run a case table through `Intl` in workerd.
  - They write `kit/i18n/testdata/golden/*.json`, and `go test` compares the Go engine against them.
  - CLDR versions must match: pin cldr-json to the CLDR major of workerd's ICU (Phase 0 finds it). Known ICU-vs-CLDR
    differences go in an explicit allowlist with reasons.
- **Second, independent oracle: [unicode-org/conformance](https://github.com/unicode-org/conformance)** (Unicode's
  "Data Driven Test").
  - Covers collation, number format, datetime format, list format, plural rules, relative date/time, display names,
    likely subtags and MessageFormat 2.
  - Test and verify files are JSON per ICU version (`testData/icuNN/*_test.json` + `*_verify.json`), generated from
    UTS-35, CLDR data and ICU test data, and run against ICU4C, ICU4J, ICU4X, NodeJS and Dart.
  - It isn't limited to Chrome's 57 locales and isn't tied to one browser. `go test` reads the JSON; their Python
    driver isn't needed.
  - Later, a Go executor could be contributed upstream (⚠ an outward-facing PR).
  - Check which ICU/CLDR versions `testData/` has for CLDR 48.
- **Oracle roles:**
  - unicode-org/conformance: breadth, many locales, reference ICU.
  - workerd `Intl`: "what our visitors' Chrome and Cloudflare actually show", used for the client-guard decisions.
  - IANA tzdata: zone offsets. Each golden records which oracle it came from.
- **Tests (`mise run check`)**
  - engine goldens
  - catalog completeness, with the same placeholders and each locale's CLDR plural forms
  - **hard-coded text:** every page and fragment rendered in `en-XA`; any untranslated text node or `placeholder` /
    `aria-label` / `title` fails
  - URL prefix audit
  - logical-class audit
  - `TestBoardFragmentWireFormat` for every locale
  - size budget
  - Workers smoke: `/de/`, `/ar/formats`, an unknown prefix (404), and a formatted date and currency byte-identical
    between native and TinyGo
  - e2e (one-app Phase 6): `ar` screenshots, `en-XA` overflow, switcher round-trip, mixed-locale two-tab push, a live
    relative-time tick

## Unknowns to settle in Phase 0

1. **workerd's ICU coverage** (confirm the cldr-is report):
   - ICU and CLDR version
   - which of our locales resolve fully: probe `de`, `ar`, `ja`, `en-IN`, plus a known gap (`is`)
   - `Intl.DurationFormat`, `Intl.Locale#getWeekInfo`, `Intl.DisplayNames`
   - its tzdata version (workerd#7256)

   The oracle is used only for covered locales; the rest use CLDR's own test data.
   - **tzdata freshness (1b):** Go's `time/tzdata` is tied to the Go release. Compare its version with IANA's latest
     and decide how we track IANA updates (bump Go, or generate zone data ourselves). — Go 1.27.1 = 2026c; IANA
     2026d came out 2026-09-11 (Inuvik). Still open.
2. **TinyGo 0.42 + `time/tzdata`:** does `time.LoadLocation` work on wasm under workerd, and what's the gzip size?
3. **Generator size per locale:** CLDR tables for `de` and `ar` (dates, numbers, currency, units, relative time,
   lists, display names), measured as gzip wasm delta.
4. **Collation:** `x/text/collate` under TinyGo (works? size?) vs a generated root collation with tailorings for
   supported locales only.
5. **Per-locale Room fan-out:** hibernation tags under local workerd and live, publish payload with K fragments, Go
   CPU time for K renders per write, `mise run load` with mixed locales.
6. **Fonts without Node or a CDN:** a self-hosted Noto Sans Arabic woff2 source. `ja` uses system fallback (CJK woff2
   files are multi-MB).
7. **gsxui attribute forwarding** for the hard-coded labels in the components we use.
8. (dropped: engine D rejected, see decision 3.)
9. **Browser ICU drift and gaps:** check the client feature-detect guard on a locale Chrome lacks. compare Chrome, Safari and Firefox output for our goldens. Where they differ from workerd,
   does the client re-format visibly change server text?

## Plan

### Phase 0: upstream demos + spikes (scratchpad only, no repo changes)

- [x] gsxui RTL page (`mise run upstream:gsxui:serve` → `/docs/rtl`), including the Arabic login example.
- [x] workerd `Intl` probe (unknown 1). Record the ICU and CLDR versions and which `Intl` APIs exist. — ICU and CLDR
      version aren't exposed; output = Chrome 152. See Findings.
- [x] TinyGo + workerd scratch Worker (workers-go v0.35.0):
  - `time/tzdata` + `LoadLocation` (unknown 2)
  - `x/text/collate`, `x/text/unicode/norm` and `x/text/language` sizes and runtime (unknown 4)
  - `fetch.NewIncomingProperties` `Timezone`/`Country` under local workerd; note what `cf` is locally
- [x] Generator prototype (unknown 3): cldr-json → `de` + `ar` date skeleton `yMMMEd`, relative time "in 3 days" /
      "yesterday", and currency `1234.5 EUR`. Compare against workerd `Intl`, measure size.
- [x] Browser ICU drift and gap check (unknown 9); tzdata versions in Go vs workerd vs IANA (unknown 1b). — Chrome
      152 and Firefox 155 done. Safari skipped (safaridriver needs admin to enable).
- [x] Room tags spike (unknown 5). — Local workerd. The live edge needs a ⚠ deploy (not done).
- [x] Write findings here; settle decisions 3, 6 and 11. **Commit the plan update.** — Decision 3 = C. Decision 6 =
      guarded browser JS (your answer). Decision 11: the +800 KB budget holds (estimate below).

### Phase 1: locale plumbing + preferences

- [x] `i18n.Context`, prefix middleware, preference resolution (cookie > `cf` > defaults), `writeNode(w, r, node)`,
      URL helper, 404 for unknown prefixes.
  - `kit/i18n` has `Request` + `WithRequest`/`FromContext`.
  - `i18n.go` `withLocale` wraps the mux: strips the prefix, 301s `/en/`, mixed case and `/de`, remembers the
    `locale` cookie, and sets `Content-Language`.
  - `views.URL` / `Loc` / `LocalePrefix` / `LocaleLinks`.
  - Unknown prefixes 404 through the normal mux.
  - `httpx.Render` already renders with `r.Context()`.
  - Time zone, currency, hour cycle and units preferences move to Phase 3/4, where their formatters land.
- [x] Layout: `<html lang dir>`, `me-auto`, and a switcher plus a preferences dialog (locale, time zone, currency,
      hour cycle, units, first day of week) from gsxui components.
  - Switcher done as a footer list of plain links (`hreflang`, `lang`, native names, `aria-current`), plus a header
    `icon.Languages` button to `#languages`.
  - gsxui dropdown items are `div role=menuitem` with synthesized clicks, so links inside them don't work from the
    keyboard.
  - The preferences dialog moves to Phase 3/4.
  - `/?locale=en` explicitly chooses the default locale (302 to `/`).
- [x] Empty `/formats` page (our demo) with a per-locale link row. — Not empty: locale facts, numbers, currency (local
      currency from the maximized region) and plural rules, on gsxui `table` (`gsxui add table`).
- [x] Tests: URL prefix audit, logical-class audit, native = TinyGo HTML. Run `mise run check`. **Commit.**
  - `i18n_test.go`: routing, cookie, noindex, localized URLs in all 13 prefixed locales × 4 pages, language links,
    logical classes, formats values.
  - Smoke: `/de/`, `/ar/formats` rtl, CLDR numbers and currency on TinyGo, localized `hx-post`, 301, 404.
  - `e2e/locale_test.go`: switch, rtl mirroring, boosted nav inside `/ar/`, remembered locale, choosing English.
- [x] Extra: `X-Robots-Tag: noindex` for untranslated locales (`translated` in `i18n.go`) until Phase 5, so English
      text under `/de/` isn't indexed.

### Phase 2: generator + engine core + oracle

Done before Phase 1: the engine lives only in new paths, so it could proceed while the one-app session held `main.go`,
views and platform files.

- [x] `cmd/cldrgen` (pinned cldr-json tag, locale list in one place) and `mise run i18n`. — `kit/i18n/cldrgen` + `cmd/cldrgen`;
      `I18N_LOCALES` in `mise.toml`; `mise run i18n:generate` writes `kit/i18n/cldr` (cldr-json 48.2.1, plus CLDR XML
      `release-48-2` for root and per-locale number-system symbols). Caches in the user cache dir, not the repo.
- [x] Locale parsing, likely subtags, parent chains and matching. — `ParseTag`, `Canonical` (aliases), `Maximize`,
      `Minimize`, `Parent`, `Data.Locale`, `Data.Match(Accept-Language)`. Matching prefers the base locale over a sibling
      region (en-GB → en, not CLDR's en-IN).
- [x] Plurals: cardinal, ordinal, range. — Rules parsed at generate time; every CLDR sample checked (64–133 per locale).
- [x] Numbers: decimal, percent, compact, numbering systems, Indian grouping. — Full `Intl.NumberFormat` minus unit style
      (Phase 4):
  - styles: decimal, percent, currency with symbol / narrow / code / name and accounting
  - notations: compact short/long, scientific, engineering
  - digits: significant and fraction, roundingPriority, all 9 rounding modes, increments, stripIfInteger
  - sign displays, useGrouping, `-u-nu`
  - money exact via `Decimal` / `Minor`
- [x] `workerd/intl-oracle.mjs`, `mise run i18n:golden`, golden tests, allowlist. — `kit/i18n/intltest`: cases in Go,
      `oracle.mjs` in a throwaway workerd, `cmd/intloracle`, `mise run i18n:golden` →
      `kit/i18n/testdata/golden/workerd.json`. `TestConformance` in `go test`; `known` allowlist (empty).
- [ ] `/formats`: numbers and plurals sections. Size budget check. **Commit.** — Committed; `/formats` and the budget
      check move to Phase 1, since they need the app.

### Phase 3: dates, times, zones, relative time, durations

Split into two parallel workstreams in the same tree with strict file ownership: `Intl.DateTimeFormat` (a subagent,
`kit/i18n/datetime*.go`, `cldrgen/datetime*.go`, `intltest/*_datetime.go`) and everything else (lead). The shared
scaffolding came first (edf4723): a generic `Ctor`/`Method`/`Args` oracle case form, data fields and generator hooks.

- [x] Date and time styles, skeleton matching, hour cycle, day periods, eras, intervals. — Subagent (3c79c1e):
      14,770 + 168 Intl cases.
- [x] tzdata, zone display names, week data. — Zones: subagent. Week data done: `Locale.WeekInfo` =
      `Intl.Locale#getWeekInfo`, CLDR weekData, `-u-fw`; 25/25 Intl cases.
- [x] Extra (decision 5): viewer time zone and hour-cycle preferences.
  - `i18n.Request.TimeZone`/`TimeZoneChosen`/`HourCycle`; cookie > `cf.timezone` > UTC.
  - Header dialog (gsxui Dialog + NativeSelect) over `/fragments/preferences` and `POST /preferences`.
  - `views.LocalTime` with a browser correction.
  - kit/i18n `Data.TimeZone`, `Data.TimeZoneIDs`, `Locale.TimeZoneName`, `DateTimeOptions.IntlJSON`.
- [x] Relative time (numeric auto/always, three widths) and the live schedule (decision 6).
  - `RelativeTimeFormat` (`Intl.RelativeTimeFormat`): all 8 units × 3 styles × numeric auto/always, `-0` in the past,
    plural unit names, `-u-nu`.
  - `ElapsedUnit` thresholds.
  - Browser `static/relative-time.js` (guarded; page `lang`; thresholds pinned by `TestRelativeTimeJSMatchesGo`).
  - Board notes render `<time datetime data-relative-time>`.
- [x] Durations. — `DurationFormat` (`Intl.DurationFormat`): styles, per-unit styles and displays, fractional
      digits, `-u-nu`. It follows V8's `DurationRecordToListOfFormattedNumber`, which differs from the spec text: a
      numeric seconds is appended to the previous item with the ICU time separator ("5 min:3.00").
- [x] Extra (needed by durations, Phase 4 scope): NumberFormat `style: "unit"`.
  - 45 sanctioned units and X-per-Y; CLDR direct units, perUnitPattern, compoundUnitPattern.
  - ICU quirks: short/narrow `percent` uses the percent number pattern and drops `-per-`; a compact unit's plural
    comes from the original value.
- [x] Extra (Phase 4 scope): `ListFormat` (`Intl.ListFormat`), with ICU's es/he rules in code.
- [ ] Goldens. `/formats` sections with a live ticking example. **Commit.** — My areas: **10,049/10,049** Intl
      cases. `/formats` has units, relative time, lists, durations and week sections. Commit waits for the
      DateTimeFormat work: the shared golden and tables hold its unfinished cases.

### Phase 4: currency, units, lists, display names, collation, text, input

- [ ] `Money` formatting (all styles, digits, cash rounding, spacing, region default).
- [ ] Units + measurement preferences.
- [ ] Lists, display names, collation (per unknown 4).
- [ ] Case mapping, NFC, grapheme truncation, bidi helpers.
- [ ] Localized decimal and currency input parsing with round-trip tests.
- [ ] Goldens and `/formats` sections. **Commit.**

### Phase 5: messages + extract every string

- [x] `cmd/msggen`: MF1 in TOML → typed Go (plural, selectordinal and select, with formatted args delegating to the
      engine). `locales/en.toml` is the source.
  - Parser in `kit/i18n/msggen`, formatter `(*i18n.Locale).FormatMessage` in `kit/i18n/message.go`, `mise run i18n:messages`.
  - Generated code: static catalogs, `Messages` with one method per key, `For`/`ForLocale`/`Pseudo`/`Complete`.
- [x] Move every string from views, handler errors and `theme.gsx` into the catalogs. Board value → number formatter,
      note times → relative schedule. `go tool gsx fmt -w`.
  - Still to do: `NowFragment` → date/time in the viewer's zone. Server-info "Rendered" is a `<time datetime>` with
    RFC 1123 text until DateTimeFormat lands.
  - Uptime → DurationFormat.
- [x] Generated pseudo-locales. Hard-coded-text and completeness tests pass. **Commit.**

### Phase 5b: message keys through gsx renderers (decision 13; waits for your go)

- [ ] Scratchpad spike on gsx v0.1.0:
  - a struct `Key` and a `Message` renderer in text, attribute, conditional attribute and gsxui fallthrough attrs;
  - an unregistered `Key` fails `gsx generate`;
  - TinyGo on workerd gives byte-identical HTML.
- [ ] msggen: `locales/msg` keys and typed constructors, `RenderKey`/`RenderMessage`, `Key.In(ctx)`; register them in
      `gsx.toml`. Keep `mise run rename` working.
- [ ] Rewrite the 103 view call sites. Owned components take `locales.Message` props. `M(ctx)` goes, or stays for
      Go-only helpers.
- [ ] `mise run check`, `mise run e2e`, wasm size delta. Update the AGENTS.md "UI text" rule. **Commit.**

### Phase 6: live board per locale

- [x] `index.mjs` locale rule + shared-rule test, Room tags, per-locale `last`, multi-fragment publish, count-only
      presence.
  - `kit/live`: `LocaleParam`, `LocalePattern`/`ValidLocale`, `Localized{Default, Fragments}.JSON()` (no
    encoding/json); `Publish` takes `Localized`.
  - `board.go` renders `BoardFragment` per shipped locale (`lang` on `#board`) and answers the poster with its own.
  - `room.mjs`: `acceptWebSocket(ws, [locale])`, `fragmentFor`, count-only presence, and a legacy `{html}` cache
    still served (found by e2e: an old cache made `fragmentFor` throw on connect).
  - Board page: `?locale=` on `hx-ws:connect`, label outside `#presence`.
  - `TestWorkerJSMatchesKitLive` checks the locale rule.
- [x] `cmd/wsload` with mixed locales. `mise run test` + `mise run load` (1,000 sockets). Record CPU per write.
      **Commit.**
  - `-locales`, and `WrongLocale` in the result. Smoke runs 3 sockets in en,de,ar (0 wrong).
  - 2026-09-14 14:50, local workerd with translated catalogs, 1,000 sockets over all 14 locales:
    - 0 wrong-locale pushes.
    - 1 write → all 1,000 sockets at p50 59 ms / p95 64 ms.
    - A 50-write burst: 1,000/1,000 at the newest version (p50 1.56 s, set by the burst's own duration).
    - Socket 1,001 is refused. e2e is green.
  - CPU per write is still open: local workerd doesn't report it. Read `cpuTime` from `mise run tail` on the next
    deploy.

### Phase 7: real locales, fonts, RTL check

- [x] Catalogs for decision-1 locales (decision 9): machine-drafted, 118/118 each. Native review pending (wording
      notes in Findings, Phase 5).
- [ ] Noto Sans Arabic fallback via the preset/assets task (unknown 6); `ja` uses system fallback.
- [ ] gsxui label gaps (decision 10). Issues filed: gsxui#31, #30, #32 (see Findings 2026-09-14 17:30). Waiting on
      upstream; when a fix lands, bump the gsxui pin, `gsxui add` the affected components, translate the new labels
      and remove their `TestNoHardcodedText` allowlist entries.
- [x] Browser check on local workerd (e2e `i18n_test.go`, `datetime_test.go`):
  - `ar` mirrored with Arabic-Indic digits: `TestArabicDigitsAndMirroring`.
  - `de` / `en-XA` without overflow: `TestNoOverflow`, 8 locales × 4 pages at 390 px and 1100 px. en-XA pseudo text is
    Go-test only; de/ru/fr/pt-PT are the long real languages. **Found:** the header nav overflowed on phones in every
    locale (421–508 px), so it now wraps.
  - switcher and preferences round-trip: `TestLocaleSwitchKeepsPreferences`.
  - mixed-locale push: `TestMixedLocalePush` (German + Arabic browsers on one topic).
  - relative-time tick, plus the calibration guard: `TestMixedLocalePush`, `TestViewerTimeZone`.

  **Commit.**

### Phase 8: SEO (joins the search plan's Phase 2)

- [x] Translated `<title>`/description, per-locale canonical, reciprocal `hreflang` + `x-default`, sitemap
      `xhtml:link` alternates. Tests assert reciprocity. **Commit.**
  - `views/seo.go`: `CanonicalURL`, `Alternates`, `Sitemap`, `SitemapPages`. `Layout` takes a description (the pages'
    translated intros, plus a new `about.description` in 14 catalogs). `/sitemap.xml` exists at the root only.
  - Origins come from `httpx.Origin` (search plan's helper). `/robots.txt` (search plan) is registered next to the
    sitemap.
  - Tests:
    - `TestCanonicalAndHreflang`: 5 pages × 14 locales, reciprocal, canonical query rules, TLS scheme.
    - `TestSitemap`: parsed XML, 56 URLs, each alternate equal to its page's.
    - `TestNoHardcodedText` now covers meta descriptions.
    - Smoke on TinyGo: canonical, x-default, sitemap alternates.
  - Split with the search plan (agreed with its session 2026-09-14). This phase owns `/sitemap.xml`, generated in Go
    from `cldr.Data.Locales` + `views.LocaleLinks` (not a static file, which would drift from `I18N_LOCALES`), plus
    canonical, hreflang/x-default and the translated description.
  - The search plan keeps static `robots.txt` (pointing at `/sitemap.xml`), `httpx.NoIndex` on `/fragments/*`,
    `/healthz` and POSTs, and OG tags built on this head (`og:locale` / `og:locale:alternate`).
  - The board's canonical keeps `?topic=` for non-default topics (each topic is its own indexable page);
    `?topic=lobby` canonicalizes to `/board`.
- [ ] After the ⚠ deploy: Search Console URL Inspection on `/de/`, `/ar/` and `/ja/formats`.
  - Deployed 2026-09-14 as 7d6915f, with search Phases 1–3: smoke-remote 26/26, live e2e 11/11.
  - Independent live checks by go-htmx4-12 passed 24/24:
    - robots.txt is ours, and the sitemap has 56 URLs;
    - no noindex on pages, noindex on fragments, `/healthz` and POSTs;
    - canonical, x-default, description and OG on `/de/about`;
    - `?topic=lobby&utm_source=x` canonicalizes to `/board`;
    - spoofed Googlebot / Google-Agent / Google-GeminiNotebook user agents get 200.
  - Still open: real-Google proof (Rich Results Test, Search Console in the user's account). Do it after the custom
    domain (go-htmx4.ubuntusoftware.net, domains plan) so the property uses the final host.
  - 2026-09-14 17:10, Search Console API (`mise run search:submit` / `search:status`, kit/searchconsole):
    - Service account `search-console@go-htmx4-search` has Full access on `sc-domain:ubuntusoftware.net`.
    - `https://go-htmx4.ubuntusoftware.net/sitemap.xml` was submitted and downloaded by Google within a second:
      56 URLs submitted, 0 errors, 0 warnings.
    - Minutes later: 46 URLs "Discovered – currently not indexed", 10 "URL is unknown to Google", no canonical
      conflicts. Re-run `mise run search:status` in a few days.
    - Test live URL and Request indexing have no API (Search Console UI). The rendered HTML must
      keep the page's language, digits and dates after client `Intl` runs.

### Phase 9: calendars (decision 7, after v1)

- [ ] `-u-ca-` Japanese, Islamic and Buddhist via the generator, with goldens and a `/formats` section.

### Phase 10: docs + kit

- [ ] AGENTS.md rules:
  - no literal user-facing text in `views/` or handlers
  - money is `i18n.Money`
  - instants are UTC
  - all URLs via the helper
  - logical classes only
  - never edit generated tables or catalogs
  - `mise run i18n` / `i18n:golden`
- [ ] README: add a locale, add an RTL locale, use `kit/i18n` from another repo.
- [ ] Engine goes to `kit/i18n` with one-app Phase 3; ⚠ deploy rides one-app Phase 7.
- [ ] Move this plan to `.plans/done/`.

## Findings

### 2026-09-14 17:30: gsxui upstream issues (decision 10)

Audited gsxui `main` at f0818be (the same as our c7fd6a8 pin for these files; only the toaster docs differ) and filed:

- [gsxui#31](https://github.com/gsxhq/gsxui/issues/31), **hard-coded English labels** that callers can't reach:
  - Dialog and Sheet close, and the DialogFooter "Close" button
  - toast close and the toaster "Notifications" landmark (plus its `toaster.js` fallback)
  - carousel prev/next, the Sidebar mobile title and description, and SidebarTrigger
  - BreadcrumbEllipsis, and pagination's visible "Previous"/"Next" and "More pages"
  - the calendar nav's `aria-label`s

  Proposed: label props with English fallbacks, or `ui.WithLabels(ctx, …)`. Literals placed before `{ attrs... }`
  can already be overridden (Spinner, the Breadcrumb/Pagination nav, SidebarRail, carousel roledescription), so
  they aren't listed.
- [gsxui#30](https://github.com/gsxhq/gsxui/issues/30), **Calendar locale.** Month and weekday names, the caption
  and day `aria-label` are English in both `calendar.gsx` and `calendar.js`. Proposed: caller-supplied names,
  placeholder patterns and digits, written by the server as data attributes that `calendar.js` reads, so the two
  stay byte-identical (gsxui's own jsx-parity.md rejects client-only `Intl`).
- [gsxui#32](https://github.com/gsxhq/gsxui/issues/32), **RTL.**
  - About 15 components still use physical classes (`pr-8`/`right-2` item indicators in select, combobox and the
    menus, `ml-auto` shortcuts and chevrons, `text-left` in table, alert and field, input-group margins, …).
  - gsxui's RTL page says components are logical, and Task 1 (the CSS pass) of their RTL plan is unchecked.
  - Only the NativeSelect chevron was confirmed in a browser; the rest come from a grep with the documented
    exceptions left out.

### 2026-09-14 16:20: follow-ups (pins tool, CI, Safari, calibration, Phase 7 browser checks)

- **Pins:**
  - `cmd/i18npins` / `mise run i18n:pins` reads a workerd release's Chromium ICU commit, ICU and CLDR version, and
    moves the pins with `-write` (tested on a scratch copy).
  - `TestPinsMatchWorkerd` is part of `i18n:verify`.
  - The oracle's `compatibilityDate` now comes from `workerd --version`, so there is no second copy of the pin.
  - The newest workerd, 1.20260914.1, still uses Chromium ICU `8cc91d9b`.
- **CI:** `.github/workflows/i18n-verify.yml` + `ci/i18n-verify.sh`, shell only, running weekly, on demand, and on
  i18n pull requests:
  - `i18n:verify`;
  - `i18npins -latest -strict`;
  - the browser drift report (informational).
  - Not pushed yet, so it hasn't run on GitHub.
- **Safari 26.6.2** through safaridriver (Remote Automation was already enabled): 4,895 of 33,301 cases differ,
  **including relative time** in zh-Hant (206), ru (6) and hi (4), e.g. "आने वाला कल" for "कल".
  - So the browser script would have changed server wording in Safari.
  - `static/relative-time.js` now calibrates. Dates are corrected only if the browser reproduces the server's text
    in the server's zone. Relative time is corrected only if the server's text is producible by this browser.
  - Both paths are e2e-tested with matching and foreign wording.
- **Phase 7 browser checks:** done (see the Phase 7 checklist), and they found the phone overflow in the header nav.

### 2026-09-14 15:55: browser drift (`mise run i18n:browsers`), Phase 0 spikes deleted

- **Chrome 152** matches the workerd golden in 33,300 of 33,301 cases. The one difference is `locale/und-Arab`'s
  resolved default locale, which follows the host (en-GB here) and isn't ICU drift. Chrome ignores `LANG` on macOS.
- **Firefox 155** differs in 887 cases:
  - datetime 622: hour-cycle extensions, day periods, ranges, zone generic names;
  - number 166, e.g. XXX shows "¤";
  - duration 70: separators;
  - unit 28, e.g. ar percent-per.
  - Relative time, lists and plurals match exactly.
- **Effect on `static/relative-time.js`:**
  - Relative time is safe in both browsers.
  - Local-time correction only runs when the browser's zone differs, and Firefox's text is then Firefox's own
    format. In the style cases the app uses, 6 of 756 differ, all h12 in 24-hour locales ("6:45 PM" where
    Chrome/kit/i18n say "06:45 PM").
  - Accepted: it is the viewer's browser formatting their own local time.
- **The spikes are gone:** `.spikes/i18n-phase0/` was deleted and its `.git/info/exclude` line removed; everything
  useful now lives in the repo (table in the Phase 0 findings).

### 2026-09-14 15:45: reference data for other developers

- **Pins:** every upstream input is pinned in `kit/i18n/cldrgen/sources.go` (cldr-json, CLDR XML, Chromium ICU,
  workerd) or `mise.toml`. `TestReferencePins` (in `mise run check`) keeps them consistent with the golden runtime,
  `I18N_LOCALES`, the tables and `intltest.Locales`.
- **Chromium ICU:** the date agent had read Chromium's ICU at `refs/heads/main`, which isn't pinned. It is now
  pinned to `8cc91d9b…` (ICU 78.2), the commit workerd 1.20260911.1 builds with.
  - The hand-copied list of 429 trimmed zones is gone. cldrgen reads `filters/common.json` at that commit, and the
    tables came out byte-identical.
- **`mise run i18n:verify`:**
  - tables regenerate byte for byte;
  - 3,458 region names match Chromium's region bundles, which covers `chromeRegionAlt`;
  - goldens re-record identically with the pinned workerd;
  - `knownEvidence` (text the pinned Chromium ICU or cldr-json has or lacks) backs every data `known` entry.
    A mutation check confirmed it fails.
  - From an empty cache it takes 1:32 and downloads 17 MB.
- **Docs:** `kit/i18n/README.md` covers sources, committed vs cached, tasks, rules, and how to move each pin or add a
  locale. It is linked from AGENTS.md and README.md. The cache location can be overridden with `I18N_CACHE`.

### 2026-09-14 15:10: Phase 3 dates in the app (viewer time zones)

- **Engine:** DateTimeFormat by the subagent. Its report:
  - Options, pattern generator, zones, ranges, V8 quirks.
  - 25 `known` entries: ICU 78.2 data predates CLDR 48.2 fixes (Hv, de GyMMMEd typo), and iso8601 isn't implemented.
  - Chrome's filtered exemplar cities reproduced in `cldrgen/datetime_chrome.go`.
  - 24,000 random option/zone runs against the oracle: 0 mismatches outside the known classes.
  - Open: non-Gregorian calendars; legacy three-letter zone ids ("PST") are a RangeError, which V8 accepts.
    Before-1970 and post-Chrome-tzdata offsets differ; Go's IANA 2026c is the correct side.
- **App:**
  - Server-info "Rendered" is `LocalTime` full/long. Board notes carry a UTC tooltip. `/formats` has dates (styles,
    skeletons, hour cycles, day periods, eras, fractional seconds, native digits), zones (8 zones × all
    `timeZoneName` styles) and ranges.
  - The header clock dialog saves `tz`/`hc` cookies. Browser correction of automatic times is checked in e2e with
    Chrome's timezone override.
- **Oracle gap found:** the app applies hour cycles to date/time styles, which had no cases. I added
  `dfull-tlong-h12/h23`, `dlong-tshort-h12` and `dshort-tfull-h23`. V8 output confirmed kit/i18n, including ja
  h12 "2026/5/10日曜日". Conformance: 33,301/33,301.
- **Size:** app.wasm 4,607,013 B raw / **1,412,108 B gzip**. That is +193 KB gzip once the app uses DateTimeFormat
  (time/tzdata plus code); the tables were already linked. The Workers Paid limit is 10 MB.
- **CPU:** `/de/fragments/preferences` builds 418 localized zone labels with two names each.
  - First version: 121 ms on local workerd, and TTFB about 250 ms above a trivial fragment on the edge. Each label
    built two DateTimeFormats and loaded tzdata twice.
  - `Locale.ZoneNamer` builds the formatter once and resolves each zone once: 34 → 13 ms natively, where zip IO in
    tests dominates, and **121 → 30 ms on local workerd**.
- **Live** (deployed 2026-09-14 15:10, https://go-htmx4.gedw99.workers.dev):
  - `cf.timezone` resolves this connection to Asia/Bangkok (GMT+7) on the server.
  - The zone list's TTFB is within noise of a trivial fragment (about 0.34 s vs 0.29–0.40 s from Bangkok).
  - `mise run smoke-remote` passes 21/21.
  - Every e2e test passes against the live Worker (`E2E_BASE=…`), including `TestViewerTimeZone` and the board,
    locale and rate-limit tests.
- **Tests:**
  - `TestTimeZonePreferences`: precedence, cookies, the POST redirect's same-site `return`, a 400 on bad values,
    and the chosen zone dropping `data-local-time`.
  - `TestTimeZoneName`: every locale × style × zone equals the Format output.
  - `TestTimeZone`, `TestDateTimeOptionsIntlJSON`.
  - Smoke on TinyGo: `Samstag, 4. Juli 2026`, `11:30 AM Eastern Time`, a PDT cookie.
  - e2e `TestViewerTimeZone`.
- **gsxui gap:** the NativeSelect chevron uses physical `right-2.5` and sits on the wrong side in RTL. Add it to the
  decision-10 list; `ui/` isn't edited.
- **Translations:** the 16 new keys in all 12 locales were drafted by subagents. Review notes:
  - "Clock" became "time format" in de/fr/es/pt/ru/ja.
  - Hindi loanwords follow the file's style.
  - French now mixes ASCII and typographic apostrophes across old and new lines.

### 2026-09-14 14:40: Phase 5 (UI message catalogs)

- **Catalogs:** 118 keys in `locales/en.toml`; de, fr, es, pt-BR, pt-PT, ar, he, ru, hi, ja, zh-Hans and zh-Hant
  complete. They were drafted by translation subagents and checked by msggen (arguments, kinds, plural categories).
  - `en-IN.toml` is empty: keys come from the CLDR parent chain (en-IN → en-001 → en), and inherited-from-source
    counts as complete.
  - Every locale is now indexable (`locales.Complete`), so the Phase 1 noindex stopgap is gone.
- **Tests:**
  - msggen: parse and format (ICU apostrophes, `=N`, `offset:`, ru few/many with decimals, selectordinal, select,
    number skeletons), parse errors, inheritance, argument checks.
  - `TestGeneratedUpToDate`, `TestShippedLocalesComplete`, `TestNoHardcodedText`, `TestElapsed` and `TestIsolate`.
  - Smoke on TinyGo: German catalog, the Russian ICU plural "до 280 символов", no `X-Robots-Tag` on `/de/about`.
  - e2e: `/ar/about` shows Arabic UI text after boosted nav.
- **Found by tests:**
  - `TestNoHardcodedText` flagged gsxui toaster `<template>` placeholders (client-side, skipped) and CLDR output on
    `/formats` (now `data-i18n="cldr"`).
  - It also caught that the greet error bypassed pseudo messages, and that the server-info date is unlocalized
    (waits for DateTimeFormat).
  - e2e found an empty uptime on Workers: DurationFormat, like V8, hides zero units, so a zero duration is "".
    `Elapsed` shows seconds always when zero.
- **Size:** app.wasm 3,966,802 B raw / 1,218,856 B gzip. That includes the in-progress DateTimeFormat tables, which
  are most of the jump from 1,066 KB.
- **gsxui gaps** (decision 10, upstream issue ⚠ needs OK):
  - toaster `aria-label="Notifications"` and toast close `aria-label="Close"` are hard-coded (allowlisted in the test).
  - `DialogContent`/`DialogFooter` hard-code "Close", worked around with our own translated footer button.
- **For native review** (agent notes):
  - fr: NBSP before `: ; !`.
  - de: Sie-free infinitives.
  - pt-PT vs pt-BR differ in vocabulary (separador/aba, partilhado/compartilhado, A carregar…/Carregando…); pt-PT
    has no settled word for "locale" ("idioma e região").
  - ar/he: `board.online` wording; "-1" may show as "1-" in RTL.
  - ar: `note_length` rephrased to avoid the noun's number agreement.
  - ru: the toast was rephrased to avoid declining `{name}`.
  - hi: `served_by` label is the weakest.
  - he: gender-neutral infinitives and plural imperatives.
  - "Room" is kept as the Durable Object class name.

### 2026-09-14 13:05: Phase 1 (locale URLs in the app)

- **Size:** the app wasm is 2,139,327 → 3,118,156 B raw, **682,114 → 983,269 B gzip (+301 KB)**. That covers kit/i18n
  + 14 locales + the `/formats` page + the gsxui table. The biggest table candidates to allowlist later are likely
  subtags (7,788) and currency names (305 × 14).
- **Bug caught by e2e:** after a locale was remembered, the language list's English link to `/` bounced back to
  `/ar/`. Fixed with `/?locale=en`: remember, then 302.
- **gsx:** `{ if … }` isn't an attribute value; use conditional attributes (`{ if cond { aria-current="page" } }`) or
  a helper.
- **Deployed** to https://go-htmx4.gedw99.workers.dev (6d86927):
  - live `/de/` and `/ar/formats` serve their locale with `Content-Language` and `noindex`
  - Hindi digits render on the edge
  - `/en/about` 301s and `/xx/` 404s
  - `smoke-remote` and the full e2e suite pass against production
- **e2e against production needed two fixes:**
  - Chrome's HTTP/3 to Cloudflare failed on this network (`ERR_QUIC_PROTOCOL_ERROR`), so e2e runs Chrome with
    `disable-quic`.
  - `waitJS` now treats a script exception as "not yet": mid-navigation the new document briefly has no
    `<html>`/`<body>`.
- **RTL screenshot** (`/ar/formats`, headless Chrome) mirrors nav, cards, table columns and the language list.
  English text inside RTL shows punctuation on the wrong side, which Phase 5 fixes.


### 2026-09-14 12:45: Phase 2 engine core (numbers, plurals, locales)

**Conformance: 8,314/8,314 cases byte-identical to Intl** (workerd 2026-09-11 = Chrome 152) across 14 locales. The cases:
26 option sets × 9 inputs, 9 rounding modes, 13 currencies × 4 displays + accounting + compact, cardinal/ordinal plurals,
plural ranges, and locale maximize/minimize.

**What reaching 100% took beyond cldr-json** (each is now in the generator or runtime):
- **Numbering systems.** Systems a locale has no data for take CLDR root's explicit `arab`/`arabext` symbols. Other
  systems alias to the locale's own latn symbols.
- **XML-only symbol overrides.** Per-locale overrides for other numbering systems exist only in CLDR XML (zh_Hant arab
  minus), and cldr-json drops them. Only approved/contributed values count; ICU skips `draft="unconfirmed"` (fr).
- **Compact patterns.** ICU picks the compact pattern by the plural category of the *displayed* number, with no `c`
  operand ("1 Million", "1,5 billion"). Explicit-value entries (`1000-count-1` → fr "mille") replace the number.
- **XXX.** It shows its ISO code in every display: CLDR's "¤", ru's "XXXX" and "(unknown currency)" are ignored.
- **pluralRanges.** A locale with range data but no entry for a pair gives `other`, not the end category.

**Locale resolution differs from Intl on purpose.** We resolve to the shipped subset (ar-SA → ar with latn digits, where
Intl's own ar-SA data uses arab digits). Shipping more regional locales is catalog/list work. Only maximize/minimize are
compared strictly.

**TinyGo 0.42 on workerd.** A scratch Worker running every non-Locale case (8,288) gave **byte-identical output to Intl**.
All 8,288 run in ~113 ms, ~14 µs per case including formatter construction. That build is 2.32 MB raw / 803 KB gzip,
including workers-go, the case list and `encoding/json`. The real app delta is measured in Phase 1.

**Generated tables:** 14 locales ≈ 1.5 MB of Go source. `data_cldr.go` (likely subtags, aliases, currency data) is the
largest file at ~480 KB. Allowlisting likely subtags is an option if the wasm budget needs it.

### 2026-09-14 12:05: live-edge Room check (deployed spike, then deleted)

`go-htmx4-i18n-spike-room.gedw99.workers.dev`, deployed with `cmd/deploy` from a clean HEAD worktree. Spike client via
`GODEBUG=http2client=0`, because Go's HTTP/2 client rejects the Upgrade header on the unsupported-locale check.

- **n=10, all pass.** Single publish delivered p50 81 ms.
- **n=1000 (200 per locale), all pass:**
  - 0 cross-locale deliveries
  - presence 1000 → 500
  - late joiners get their locale's cache
  - single publish p50 137 ms / p95 199 ms
  - 5-version burst p50 1.2 s / p95 3.5 s, in line with the untagged Room's earlier live result (p95 3.58 s, limited by
    this network)
- Worker deleted afterwards; its URL returns 404.


### 2026-09-14 11:45: Phase 0 spikes (scratchpad `phase0/`)

The `phase0/…` paths below were deleted. Their replacements are in "Where the Phase 0 code went" at the end of this
entry.

**Intl probe.** Scripts are in `phase0/intl-probe/`: `probe.js`, run as a workerd Worker and by a Go collector for
headless browsers. The case table covers 82 checks × 15 locales.

- **workerd 1.20260911.1 = Chrome 152 exactly** for all 14 v1 locales, with 0 differing outputs. So the oracle
  represents Chrome users too.
- **`is` falls back** in workerd and Chrome for DateTimeFormat, NumberFormat, RelativeTimeFormat, ListFormat,
  Collator, DisplayNames and DurationFormat. The fallback is **the host's default locale**, not `en-US`. Firefox 155
  has full `is` data. This confirms the ICU filter findings below.
- **Local workerd uses the host's locale and time zone** (`en-AU`, `Asia/Bangkok` here), not UTC. So goldens and app
  code must always pass locale and time zone explicitly and never rely on runtime defaults.
- **Chrome/workerd trims are visible in output:** region names for `001`/`150` come back as raw codes; Firefox gives
  "world"/"Europe" ("Welt"/"Europa"). Script `Hant` is "Traditional" vs Firefox's "Traditional Han".
- **Chrome vs Firefox:** 145 of 1,230 outputs differ.
  - Hour + zone name patterns in es, pt, he and ru ("21 h hora estándar de Japón" vs "21 hora estándar de Japón").
  - Region and script display names.
  - PluralRules resolved locale (`en-IN`→`en`).
  - So visitors see different text in different browsers. **The client re-format must stay limited to time-zone and
    elapsed-time text.**
- **Stale time zones everywhere in `Intl`:** workerd, Chrome 152 and Firefox 155 all show Casablanca as GMT+1 on
  2026-10-01. IANA 2026c (released 2026-07-08) says: "Morocco plans to move back to permanent UTC … on 2026-09-20".
  **The oracle is not authoritative for zone offsets.** Zone goldens come from IANA data (Go's tzdata), not `Intl`.
- `Intl.DurationFormat` exists in all three. Temporal exists in Chrome and Firefox but not in workerd.
- Safari was not probed (safaridriver needs `--enable` with admin rights).

**TinyGo 0.42 + x/text v0.42.0 under workerd** (`phase0/tinygo-sizes/`). All variants build and run without panics.
gzip deltas are against a 323 KB baseline:

| Variant | gzip delta |
|---|---|
| `time/tzdata` | +128 KB (without it, `LoadLocation` fails on wasm) |
| `language` (matcher) | +69 KB |
| `feature/plural` | +45 KB |
| `unicode/norm` | +45 KB |
| `cases` | +87 KB |
| `message` (no panic) | +94 KB |
| **`collate`** | **+589 KB** |
| everything together | +794 KB |

- **Go 1.27.1 embeds IANA 2026c.** Casablanca is correct (+00 from 2026-09-20). So Go is fresher than workerd and the
  browsers today.
- **IANA 2026d was released 2026-09-11** ("Northwest Territories … stay on -06 year-round", `America/Inuvik`). Go
  1.27.1 will be wrong for Inuvik from 2026-11-01. We need a way to track IANA faster than Go releases (unknown 1b).
- **Matcher gotcha:** `x/text/language` matches `en-GB` → `en-IN` (both are children of `en-001`). The supported list
  needs `en-001`/`en-GB`, or custom matching.
- `x/text/message` `%v` of a German float printed scientific notation. Not verified on standard Go, and not used by
  our design anyway.

**Room with locale tags** (`phase0/room-tags/`, local workerd). **Viable.**

- One DO per topic and one tag per socket (limit: 10 tags, 256 chars, 32,768 sockets per DO).
- A JSON publish `{version, fragments}` with all locales; `last:<locale>` cache; count-only presence.
- n=1000 across 5 locales, 3 runs:
  - 0 cross-locale deliveries and 0 misses
  - single publish delivered p50 11–34 ms
  - still correct after hibernation eviction (new instance, 1,000 tagged sockets restored)
- Tags can't change after accept, so switching locale means reconnecting (fits hx-ws). Closed sockets linger in
  `getWebSockets()`, so the `readyState < CLOSING` filter stays.
- The 13–14 locale publish body is 35–45 KB, far under limits. The Go publisher must use `SetEscapeHTML(false)` (+31%
  otherwise).
- The locale list lives in two places (Go and JS), so the shared-rule test is required. Deploy Go and the Worker
  together.
- Not yet measured: the live edge (needs ⚠ deploy) and Go CPU for rendering K fragments per write.

**gsxui RTL demo** (`mise run upstream:gsxui:serve`, `/docs/rtl`, screenshot in `phase0/rtl/`). The Arabic sign-in
card (unmodified Card, Label, Input, Button under `dir="rtl"`) mirrors correctly: right-aligned title and labels, and
placeholders on the right.

**CLDR engine prototype** (`.spikes/i18n-phase0/cldr-proto/`).

- **Scope:**
  - generator `cmd/cldrgen`: cldr-json 48.2.1 → Go tables
  - stdlib-only runtime `i18n/`: no reflection, no runtime JSON, no `fmt`
  - features: plurals with rule parsing, numbers and numbering systems, currency (standard + accounting), date styles
    + exact skeletons, relative time, lists
- **392/392 matches against workerd** for all 14 locales, and also 392/392 against Chrome and Firefox.
  - TinyGo under workerd output is byte-identical to `go test`.
  - All 1,140 CLDR plural samples pass the runtime evaluator.
- **Engine quirks ICU adds on top of CLDR** (both implemented; the oracle and allowlist stay essential for more):
  - V8/SpiderMonkey swap U+202F → U+0020 in `DateTimeFormat` output (crbug.com/1414292), so there's a
    `WebCompatDateSpaces` option.
  - ICU's ListFormatter hard-codes Hebrew "ו-" before non-Hebrew items and Spanish y→e, o→u.
- **The browsers' ICU uses CLDR 48:** the same generator on cldr-json 47.0.0 scored 391/392 (zh-Hant `yMMMEd`).
  cldr-json also has a `48.2.1-tz2026c` tag (relevant to unknown 1b).
- **Size:** ~3.2 KB gzip per locale for this scope (ar 3.6 KB). Engine code is ~10 KB.
  - TinyGo size is noisy: importing the engine *shrank* the baseline. So the size check measures the real app
    binary.
  - Estimate for the full feature matrix: 10–30 KB gzip per locale, so 14 locales ≈ 400–500 KB gzip plus tzdata
    (+128 KB). Collation comes on top (`x/text/collate` +589 KB, or generated). **Inside the +800 KB budget only if
    collation is generated and trimmed, or scoped out.** That goes to Phase 4.
- **CPU:** all 28 cases × 14 locales render in ~0.64 ms under workerd (~1.6 µs per value, ~8× native).
- **Remaining big pieces:**
  - full skeleton matching (a port of ICU's DateTimePatternGenerator, ~1–1.5k lines; the main code risk)
  - time zone names and metazones
  - intervals
  - units, including compound units and preferences
  - compact numbers
  - display names (allowlist)
  - **collation:** cldr-json has none, so it needs CLDR XML `common/collation` + DUCET, or `x/text/collate`
  - grapheme segmentation: Unicode data files
- **Build gotcha:** generated file names must avoid Go's `_<os>`/`_<arch>` suffixes, or the build silently skips them.

**unicode-org/conformance data.** Generated test data isn't in the repo (a `gcs-upload` workflow publishes it). The
repo has generator inputs per ICU version, `testgen/icu67` … `testgen/icu78` (ICU 78 = CLDR 48):

- `collationtest.txt` and `CollationTest_*`
- `numberpermutationtest.txt` and `numberformattestspecification.txt`
- `plurals.xml` / `ordinals.xml` / `pluralRanges.xml`
- `likelySubtags.txt`, `localeDisplayName.txt`
- MF2 WG tests (currency, date, datetime, number, percent, offset…)
- `datetime.json` in icu76/77, but not icu78

The Python generators cover collation, datetime, likely subtags, display names, MF2, numbers, plurals and segmenter.
**Relative time and list data are generated with Node (`rdt_fmt_gen.js`, `list_fmt_gen.js`), which we can't run.**
Use the Python-generated sets plus the raw ICU test files; relative time and lists rely on the workerd oracle.

**Where the Phase 0 code went** (all ported; `.spikes/i18n-phase0/` deleted 2026-09-14):

| Spike | Now |
|---|---|
| `intl-probe/probe.js` (case table) | `kit/i18n/intltest` cases (one Go-owned list) + `intl.mjs` evaluation + `mise run i18n:golden` |
| `intl-probe/worker.mjs` + `config.capnp` | `intltest.Oracle` (throwaway workerd, pinned `TZ`/`LANG`) |
| `intl-probe/collector` (headless Chrome/Firefox) | `intltest.Browser` + `cmd/intlbrowsers` → `mise run i18n:browsers` (the same `intl.mjs` in the browser, drift report per area) |
| `room-tags/` (per-locale sockets, Room tags) | `worker/room.mjs`, `cmd/wsload -locales`, `kit/wsload` |
| `tinygo-sizes/run.sh` | the gzip budget in `mise run build` |
| `tinygo-sizes/variants/*` (x/text sizes) | numbers above; rebuild a variant if Phase 4 needs a fresh collate measurement |
| `cldr-proto/` | `kit/i18n/cldrgen` + `kit/i18n` |
| `rtl/` (screenshot) | `e2e/locale_test.go` (RTL mirroring) |

The browser drift check stayed outside chromedp and e2e. It launches the browser binary like the workerd oracle, so
Firefox works too and the e2e module needs no dependency on the app.

**What the probe can't validate on its own:**
- zone offsets (stale tzdata in every `Intl`)
- locales outside Chrome's 57 (they fall back)
- fields Chrome trims
- ICU-vs-CLDR-48 version differences

unicode-org/conformance and IANA data cover those.

### 2026-09-14: Chrome / Workers `Intl` gaps (from Chromium's ICU filter; behaviour still to confirm in Phase 0)

Source: [`chromium/deps/icu` `filters/common.json`](https://chromium.googlesource.com/chromium/deps/icu/+/refs/heads/main/filters/common.json),
the desktop filter behind V8's ICU data. [cldr-is](https://github.com/gudrodur/cldr-is) reports workerd and the live
edge match desktop Chrome exactly.

**By language.**
- **153 languages** get the basic locale tree (Chrome's Accept-Languages list).
- **Only 57 of them** also get currency, region, unit and time zone data:
  `af am ar bg bn ca cs da de el en es et fa fi fil fr gu he hi hr hu id it ja kn ko lt lv ml mr ms nb nl pl pt ro ru sk
  sl sr sv sw ta te th tr uk ur vi zh` (plus legacy aliases `in iw mo no sh tl`). Collation has roughly the same list.
- **95 languages are partial.** For these, cldr-is measured `DateTimeFormat`, `NumberFormat`, `RelativeTimeFormat`,
  `ListFormat` and `Collator` falling back to `en-US` (only `PluralRules` right):
  `ak an as ast ay az be bho bm br bs ceb chr ckb co cy doi dv ee eo eu fo fy ga gd gl gn ha haw hmn ht hy ia ig ilo is
  jv ka kk km kok kri ku ky la lb lg ln lo lus mai mg mi mk mn mni mt my ne nn nso ny oc om or pa ps qu rm rw sa sd si
  sm sn so sq st su tg ti tk tn to ts tt tw ug uz wa wo xh yi yo zu`.
- **Every other CLDR locale has no data at all.**

**Trimmed even for the 57 full languages.**
- Relative weekday fields ("last Monday", "next Tue") are removed.
- Localized time zone names for ~420 zones are removed, so output falls back to GMT offsets.
- Unit names: grammatical case and gender are dropped, and names are allowlisted.
- Currency narrow symbols and plural names are allowlisted.
- Macro-region names (World, Europe, Latin America…) are removed.
- Language display names exist only for the Accept-Languages set.
- `measurementSystemNames`, `personNames` and exemplar characters are removed.
- Chinese `unihan`/`big5han`/`gb2312han` collations and UCA rules are removed.
- Transliteration is limited to Latin-ASCII and kana.
- Plus workerd's stale tzdata ([workerd#7256](https://github.com/cloudflare/workerd/issues/7256)).

**Consequences for this plan.**
- The v1 locales (`en`, `en-IN`, `de`, `ar`, `ja`) are in the full 57, but still hit the trims above.
- Engine C, generated from full CLDR, has none of these gaps.
- The workerd oracle is valid only for the 57 languages and only for untrimmed fields. The client `Intl` guard (see
  "Browser `Intl` and Google") must treat the 95 partial languages as unsupported.
- Other browsers trim ICU differently: `mise run i18n:browsers` measures Chrome and Firefox (Safari needs
  safaridriver enabled with admin rights).
