package i18n

// DateTimeData is a locale's calendar (Gregorian) and time zone display data for Intl.DateTimeFormat.
// Filled in by the DateTimeFormat work (plan Phase 3).
type DateTimeData struct{}

// TimeZoneData is the shared time zone data (metazones, primary zones) for Intl.DateTimeFormat.
type TimeZoneData struct{}
